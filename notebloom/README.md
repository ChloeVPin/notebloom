# Notebloom Source

This directory contains the Wails application source code.

For the current public release notes, download links, and feature list, see the repository root [`README.md`](../README.md).

For version history, see the repository root [`CHANGELOG.md`](../CHANGELOG.md).

Development still happens from this directory:

```bash
cd frontend
npm install
cd ..
wails dev
```
